name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "0 2 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: Dr-A-Soni/badge-readme@master
          CREDLY_USER: <ami-soni.f526dd7b_credly> # optional, but default will use the same from github
